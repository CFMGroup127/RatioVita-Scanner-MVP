import CoreGraphics

enum MediaCoreLayout {
    /// Fixed macOS Media Core sidebar (section picker + list) — prevents column eating the window.
    static let macSidebarWidth: CGFloat = 300
    static let detailMinWidth: CGFloat = 420
}
